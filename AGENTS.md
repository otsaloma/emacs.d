# AGENTS.md

- Always update @straight/versions/default.el when adding or removing packages
- Keep functions and setq variables etc. in alphabetical order
- Use `/usr/bin/emacs` (not `emacs`) to run or compile code

## References

- Built-in packages: @/usr/share/emacs/30.2/lisp/emacs-lisp
- Straight.el packages: @straight/build
